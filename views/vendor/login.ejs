<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="../../styles/vendor/login.css">
    <!-- SweetAlert2 -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>

<body>
    <div class="page-title">Login Page</div>
    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Consectetur eveniet aspernatur laborum at, facilis quibusdam consequatur necessitatibus dignissimos reiciendis sunt, eum quos maiores accusamus quaerat recusandae ratione voluptates. Modi, corporis.</p>

    <div class="container">
        <div class="header">
            <h1 class="main-title">Log In</h1>
            <p class="subtitle">Log In to your Vendor Account</p>
        </div>

        <form>
            <div class="form-group">
                <label class="form-label" for="email">Brand Email address</label>
                <input type="email" id="email" class="form-input email-input">
            </div>

            <div class="form-group">
                <label class="form-label" for="password">Password</label>
                <div class="password-container">
                    <input type="password" id="password" class="form-input password-dots" value="@Safvan9633">
                </div>
            </div>

            <div class="forgot-password">
                <a href="/vendor/forgot-password" class="forgot-password-link">Forgot password?</a>
            </div>

            <button type="submit" class="login-button">Log in</button>

            <div class="signup-link-container">
                New vendor? <a href="/vendor/signup" class="signup-link">Sign up</a>
            </div>
        </form>
    </div>

    <script type="module">
        //axios
        import axios from "https://cdn.jsdelivr.net/npm/axios@1.6.7/+esm";
        //validation
        import { validateLogInForm } from "/js/utils/formValidator.js";
        // Form submission
        document.querySelector('form').addEventListener('submit', async (e) => {
            e.preventDefault();

            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            //forntend validation
            const { isValid, messages } = validateLogInForm(email, password);
            if (!isValid) {
                Swal.fire({
                    icon: 'error',
                    title: 'Validation Error',
                    html: messages.join('<br>'),
                    customClass: {
                        confirmButton: "swal-confirm-black"
                    }
                });
                return;
            }

            //Data send
            try {
                const response = await axios.post("/vendor/login", { email, password });
                const result = response.data;
                if (result.success) {
                    Swal.fire({
                        icon: 'success',
                        title: 'Login successful!',
                        timer: 1500,
                        showConfirmButton: false,
                        customClass: {
                            confirmButton: "swal-confirm-black"
                        }
                    }).then(() => {
                        window.location.href = result.redirectUrl;
                    });
                }
            } catch (error) {
                Swal.fire({
                    icon: 'error',
                    title: 'Login failed',
                    text: error.response?.data?.message || 'Invalid credential',
                    customClass: {
                        confirmButton: "swal-confirm-black"
                    }
                });
            }
        });


    </script>
</body>

</html>